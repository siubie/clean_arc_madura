import 'package:clean_arc_madura/core/usecase/usecase.dart';
import 'package:dartz/dartz.dart';

class Login implements UseCase<Either, Object> {
  @override
  Future<Either> call({Object? param}) {
    // TODO: implement call
    throw UnimplementedError();
  }
}
